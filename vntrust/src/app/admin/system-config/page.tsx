// Admin console wrapper — tái dùng trang /dashboard/system-config (API & logic giữ nguyên).
import Inner from "@/app/dashboard/system-config/page";
export default function Page() { return <Inner />; }
