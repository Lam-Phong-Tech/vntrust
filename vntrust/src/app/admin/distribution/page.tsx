// Admin console wrapper — tái dùng trang /dashboard/distribution (API & logic giữ nguyên).
import Inner from "@/app/dashboard/distribution/page";
export default function Page() { return <Inner />; }
