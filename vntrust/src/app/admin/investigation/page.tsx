// Admin console wrapper — tái dùng trang /dashboard/investigation (API & logic giữ nguyên).
import Inner from "@/app/dashboard/investigation/page";
export default function Page() { return <Inner />; }
