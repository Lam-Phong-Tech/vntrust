workflows:
  deployment-disabled:
    name: Deployment disabled
    max_build_duration: 5
    environment:
      node: 20.19.1
    scripts:
      - name: Skip VPS deployment
        script: |
          echo "Codemagic VPS deployment is disabled."
          echo "Use the external VPS deployment pipeline instead."
