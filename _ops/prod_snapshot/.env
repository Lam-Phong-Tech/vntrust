DATABASE_URL="file:./dev.db"

# Gmail SMTP — dùng App Password (không phải mật khẩu Gmail thường)
# Vào: Google Account → Security → 2-Step Verification → App passwords → Tạo mới
GMAIL_USER="spyderrhq@gmail.com"
GMAIL_APP_PASSWORD="dpzu ftvo uqcq umkz"
